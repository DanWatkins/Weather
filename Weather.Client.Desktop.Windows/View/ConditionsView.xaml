<UserControl x:Class="Weather.Client.Desktop.Windows.View.ConditionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Weather.Client.Desktop.Windows.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="BlanchedAlmond">
    <StackPanel Orientation="Vertical" Background="White">
        <Label Content="{Binding LocationName, FallbackValue='Hugo, MN'}" />
        <Label Content="{Binding GeneralSummary, FallbackValue='Partly Cloudy'}" FontSize="34" />
        <Label Content="{Binding WindSummary, FallbackValue='Wind WSW 6 mph'}" />
        <Label Content="{Binding HumiditySummary, FallbackValue='Humidity 64%'}" />
        <Label Content="{Binding VisibilitySummary, FallbackValue='Visibility 7 miles'}" />
    </StackPanel>
</UserControl>
